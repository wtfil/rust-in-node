{
  "name": "embed",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-rust": "(cd rust && cargo build --release)",
    "build-cpp": "(cd src/addon && node-gyp configure build)",
    "build": "npm run build-rust && npm ru build-cpp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ffi": "^1.3.2"
  },
  "devDependencies": {
    "benchmark": "^1.0.0"
  }
}
